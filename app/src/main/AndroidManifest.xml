<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <queries>
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE" />
        </intent>
        <intent>
            <action android:name="android.media.action.VIDEO_CAPTURE" />
        </intent>
        <intent>
            <action android:name="android.provider.MediaStore.Audio.Media.RECORD_SOUND_ACTION" />
        </intent>
    </queries>

    <!-- Permisos de cámara -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo_trasstarea"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_trasstarea"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppGestionTareas">
        <activity
            android:name=".ui.tarea.DescripcionActivity"
            android:exported="false"
            android:theme="@style/Base.Theme.AppGestionTareas.NoActionBar" />
        <activity
            android:name=".ui.helpers.BaseFilePickerActivity"
            android:exported="false" />
        <activity
            android:name=".ui.prefs.SettingsActivity"
            android:exported="false"
            android:label="@string/title_activity_settings" />
        <activity
            android:name=".ui.tarea.update.EditarTareaActivity"
            android:exported="false"
            android:theme="@style/Base.Theme.AppGestionTareas.NoActionBar" />
        <activity
            android:name=".ui.tarea.create.CrearTareaActivity"
            android:exported="false"
            android:theme="@style/Base.Theme.AppGestionTareas.NoActionBar" />
        <activity
            android:name=".ui.tarea.list.ListadoTareasActivity"
            android:exported="false" />
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:theme="@style/Base.Theme.AppGestionTareas.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="pmdm.jmh.app_gestion_tareas.FileProvider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/paths" />
        </provider>
    </application>

</manifest>